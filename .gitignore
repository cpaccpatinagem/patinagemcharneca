.DS_Store
.claude/
*.log

# sem dependências, mas por via das dúvidas
node_modules/

# trabalho interno que nao deve ir para o repositorio publico
.local/

# Fotos em alta resolução: ficam só no computador, não são publicadas.
# As do pavilhão são de crianças e trazem GPS no EXIF - mais uma razão
# para o original nunca ir para um repositório público.
assets/img/equipa/_originais/
assets/img/pavilhao/_originais/

# Vídeo em bruto do telemóvel: são crianças em treino, com GPS e data no
# ficheiro, e centenas de MB. Nunca vai para o repositório público. O vídeo do
# hero, quando existir, entra já cortado e comprimido em assets/video/.
Video intro website CPACC homepage/
*.MOV
*.mov
